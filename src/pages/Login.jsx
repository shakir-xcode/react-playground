import React, { useState } from "react";
import UserForm from "../components/UserForm";

const Login = () => {
  return (
    <div>
      <UserForm formTitle="login"></UserForm>
    </div>
  );
};

export default Login;
